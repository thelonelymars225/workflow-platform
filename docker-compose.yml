services:
  workflow-api:
    build:
      context: .
      dockerfile: services/Workflow.Api/Dockerfile
    ports:
      - "5159:8080"
    environment:
      ASPNETCORE_HTTP_PORTS: "8080"

  notification-worker:
    build:
      context: .
      dockerfile: services/Notification.Worker/Dockerfile
